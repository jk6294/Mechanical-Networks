function [] = visualize_network(Xs, Xu, conn)
% Function for visualizing the completed network
%
% Inputs:
% Xs: d x ns matrix of ns specified nodes in d dimensions
% Xu: d x nu matrix of nu unspecified nodes in d dimensions
% conn: s x 2 matrix of bonds between sp. node i and unsp. node j
%
% Output
% Figure. Assumes figure is already available

% Inigial Values
d = size(Xs,1);

end